<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "sample app", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t("static-pages.home.title"), root_path %></li>
        <li><%= link_to t("static-pages.help.title"), help_path %></li>
        <% if logged_in? %>
          <li><%= link_to t("static-pages.users.new.user"), "#" %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("static-pages.users.new.profile"), current_user %></li>
              <li><%= link_to t("static-pages.users.new.setting"), "#" %></li>
              <li class="divider"></li>
              <li>
                <%= link_to t("static-pages.users.new.log_out"), logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("static-pages.users.new.log_in"), login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
